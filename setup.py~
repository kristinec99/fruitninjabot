#
#   Python Setup
#
from distutils.core          import setup
from catkin_pkg.python_setup import generate_distutils_setup

# Specific the 'packages' as either folders or files under the lib dir.
#setup_args = generate_distutils_setup(
#    packages=['kinematics'],
#    package_dir={'': 'lib'})

setup_args = generate_distutils_setup(
    packages=['hw6code'],
    package_dir={'': 'lib'})

setup(**setup_args)
